my-app folder Initial Setup (Frontend)
1. Install git from https://git-scm.com/downloads
2. Install node.js from https://nodejs.org/en
3. Create a folder to be used for this project and open in Visual Studio Code
    -> Click File -> Open Folder
4. Run: git init
5. Run: git remote add origin https://github.com/lovgrandma/scaling_from_zero
6. Install React 
7. npm run start

Setup git
1. git branch --set-upstream-to origin/master
2. git pull

Learn React
https://react.dev/learn

Git Help

# Shows all branches
git branch -a

# Create new branch
git checkout -b <name_of_my_branch>

# Add changes
git add .

# Commit changes
git commit -a

# Checkout specific branch
git checkout 

# Pull new changes
git pull